ClusterConfigurations: # Settings for Redshift cluster
  redshift:
    userName: redshiftmasteruser
    dbName: dev
    nodeType: dc2.large # Redshift instance type
    clusterType: multi-node # Set to 'multi-node' or 'single-node'
    nodeCount: 2 # Number of nodes if multi-node
